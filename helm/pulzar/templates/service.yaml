apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulzar.fullname" . }}
  labels:
    {{- include "pulzar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pulzar.selectorLabels" . | nindent 4 }}
